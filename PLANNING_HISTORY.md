# World Room 기획 히스토리

## 이 문서의 목적

이 문서는 World Room이 어떻게 기획되었는지, 어떤 부분이 사용자 요청에서 출발했고 어떤 부분이 Codex가 제안한 제품 해석인지 구분하기 위해 작성한다.

결론부터 말하면, **World Room의 출발 요청은 사용자에게서 나왔고, 구체적인 제품 콘셉트와 기능 구조는 Codex가 제안한 뒤 사용자가 선택하거나 승인하면서 형성되었다.** 다만 초기 구현 단계에서는 Codex가 빠르게 해석해 제품 방향을 먼저 제시한 부분이 많았고, 사용자가 기획에 충분히 개입했다는 느낌을 받기 어려웠을 수 있다.

## 최초 사용자 요청

사용자의 최초 요청은 다음과 같은 요구였다.

- 창의적인 실시간 오디오 앱을 만든다.
- 이름은 **World Room**이다.
- 사용자는 말로 대화한다.
- 상대는 세계관 창작 동반자다.
- 설정, 인물, 갈등, 장면 훅을 함께 만든다.
- OpenAI의 최신 Realtime API 패턴을 사용한다.
- 오디오 입출력이 중심이어야 한다.
- transcript 표시가 가능하면 포함한다.
- 브라우저 책임과 서버/session-token 책임을 분리한다.
- 개발자 노트와 검증 체크리스트를 포함한다.
- UI는 마이크/세션 상태가 명확하고 polished해야 한다.
- OpenAI Docs와 Frontend 관련 지침을 사용한다.

이 요청만으로도 “실시간 음성 세계관 창작 앱”이라는 큰 방향은 사용자에게서 나온 것이다.

## Codex가 먼저 해석해서 제안한 부분

초기 구현에서 Codex가 주도적으로 정한 부분은 다음과 같다.

- World Room을 **한국어 실시간 음성 세계관 작업실**로 해석했다.
- Realtime API는 WebRTC 기반으로 구성했다.
- 서버는 `OPENAI_API_KEY`를 숨기고 ephemeral client secret만 발급하도록 설계했다.
- UI는 “어두운 작가 작업실 / 세계 단서 패널 / 대화 기록 패널” 형태로 만들었다.
- 오른쪽 패널에 **세계 단서**와 **장면 열기** quick prompts를 배치했다.
- 대화 중 `설정:`, `인물:`, `갈등:`, `장면:`, `훅:` 라벨을 감지해 단서로 모으는 방식을 제안했다.
- 최초 저장 기능은 로컬 `sessions/*.json` 파일 저장으로 구현했다.

이 부분들은 사용자가 세부적으로 지시한 것이 아니라, Codex가 요구사항을 만족시키기 위해 제품 형태를 구체화한 것이다.

## 사용자가 명시적으로 선택하거나 승인한 부분

초기 앱이 만들어진 뒤, 사용자는 저장과 기능 기획에 대해 함께 논의하자고 했다. 그 과정에서 사용자가 명시적으로 선택한 결정은 다음과 같다.

### 제품 방향

Codex가 세 가지 방향을 제안했다.

1. 작가 작업실
2. 즉흥 놀이방
3. TRPG 보조도구

사용자는 **1번, 작가 작업실**을 선택했다.

이 결정으로 World Room은 단순한 음성 놀이 앱이 아니라, 저장한 세계관을 계속 이어 쓰는 창작 도구 방향으로 정해졌다.

### v1 핵심 기능

Codex가 다음 선택지를 제안했다.

1. 저장한 세계를 다시 불러와 이어 말하기
2. 저장된 내용을 카드처럼 정리하고 편집하기
3. 세션 종료 후 자동 줄거리 요약본 만들기

Codex는 **1번 + 3번**을 v1 핵심으로 묶자고 추천했고, 사용자는 이를 선택했다.

따라서 v1의 핵심 기억 루프는 다음과 같이 정해졌다.

- 대화한다.
- 저장한다.
- 자동 요약한다.
- 다음에 불러와 이어 말한다.

### 저장 버튼의 의미

Codex가 자동 요약 생성 시점을 제안했다.

1. 세계 저장 버튼을 누를 때마다 자동 생성
2. 세션 종료 버튼을 누르면 자동 생성
3. 별도 정리하기 버튼으로 생성

사용자는 **1번**을 선택했다.

그래서 **세계 저장 버튼 = 원본 저장 + 요약 생성 + 다음 세션 준비**라는 의미가 되었다.

### 최근 세계 UI

Codex가 저장한 세계를 다시 이어 말하는 UI 방식을 제안했다.

1. 왼쪽에 저장된 세계 목록 추가
2. 첫 화면에 최근 세계 3개 카드 표시
3. 드롭다운으로 선택

사용자는 **2번**을 선택했다.

그래서 앱 첫 화면에는 최근 저장한 세계 3개가 카드로 나타나고, 각 카드에서 이어 말하기를 선택하는 방향이 정해졌다.

### 자동 요약 방식

Codex가 요약 생성 방식을 제안했다.

1. 서버가 저장 시 OpenAI Responses API로 요약 생성
2. Realtime 세션 안에서 모델에게 요약을 말하게 하고 저장

사용자는 **1번**을 선택했다.

그래서 Realtime은 실시간 대화에 집중하고, 저장 시 정리/요약은 별도 Responses API 호출로 처리하는 구조가 되었다.

### 저장소 선택

Codex는 처음에 로컬 JSON 파일 유지를 추천했다. 이후 사용자가 “Supabase는 어때?”라고 물었다.

이 질문을 계기로 저장소 방향이 바뀌었다.

Codex는 Supabase가 장기적으로 더 적합하다고 설명했고, 사용자는 “좋아”라고 동의했다. 이후 Supabase 기반으로 다음 구조가 잡혔다.

- `worlds`
- `sessions`
- `canon_cards`

이 결정은 사용자의 질문에서 시작되었고, Codex가 장단점을 설명한 뒤 사용자가 받아들인 것이다.

## 현재 제품 정의

현재 World Room은 다음과 같이 정의할 수 있다.

> World Room은 OpenAI Realtime API를 음성 대화 엔진으로 사용하고, Supabase를 기억 저장소로 사용하는 한국어 실시간 세계관 창작 작업실이다.

핵심 경험은 다음과 같다.

1. 사용자가 말로 세계를 만든다.
2. Realtime 모델이 낮은 지연으로 음성 응답한다.
3. 대화 기록과 세계 단서가 화면에 쌓인다.
4. 사용자가 **세계 저장**을 누른다.
5. 서버가 Responses API로 세션을 요약한다.
6. Supabase에 세계, 세션, 캐논 카드가 저장된다.
7. 앱 첫 화면에 최근 세계 3개가 카드로 표시된다.
8. 사용자는 저장한 세계를 선택해 이어 말할 수 있다.

## 사용자 관여도에 대한 정리

사용자가 관여한 부분은 다음과 같다.

- 앱의 최초 이름과 큰 요청: **World Room**
- 실시간 오디오 기반 세계관 동반자라는 큰 방향
- 한국어로 만들자는 방향
- Home_manual이 아닌 새 폴더로 분리해야 한다는 정정
- 저장 기능이 필요하다는 문제 제기
- 작가 작업실 방향 선택
- 이어 말하기 + 자동 요약을 v1 핵심으로 선택
- 저장 버튼 시점에 자동 요약 생성 선택
- 최근 세계 3개 카드 UI 선택
- Responses API 기반 요약 선택
- Supabase 도입 제안
- GitHub private repo 푸시 요청

Codex가 주도한 부분은 다음과 같다.

- 초기 UI/UX 구성
- Realtime WebRTC 구현 방식
- 서버 token 발급 구조
- 세계 단서 추출 라벨 방식
- 저장 데이터 모델 초안
- Supabase 테이블 구조 초안
- Responses API 요약 JSON 구조
- 최근 세계 카드와 이어 말하기 구현 방식

따라서 사용자는 큰 방향과 주요 의사결정에는 관여했지만, 초기 제품 콘셉트와 세부 UX는 Codex가 빠르게 제안하고 구현한 비중이 크다.

## 앞으로의 기획 방식 제안

사용자가 “내가 관여한 게 없는 것 같다”고 느낀 지점은 중요하다. 앞으로 World Room 기획은 다음 방식으로 진행하는 것이 좋다.

1. Codex가 바로 구현하지 않고 먼저 선택지를 제시한다.
2. 사용자가 제품 정체성, 저장 단위, 화면 우선순위를 고른다.
3. 결정된 내용을 `PLANNING_HISTORY.md` 또는 별도 PRD에 기록한다.
4. 그 다음에만 구현한다.
5. 구현 후에는 “사용자가 결정한 것 / Codex가 설계한 것 / 아직 미정인 것”을 다시 정리한다.

이 방식이면 World Room은 단순히 Codex가 만든 앱이 아니라, 사용자와 Codex가 함께 만든 창작 도구로 기록될 수 있다.

## 다음에 작성하면 좋은 문서

- `PRODUCT_VISION.md`: World Room이 어떤 창작 도구인지 정의
- `DATA_MODEL.md`: Supabase 테이블과 저장 의미 정리
- `V1_FEATURE_SPEC.md`: v1 기능 범위와 제외 범위
- `PROMPT_DESIGN.md`: Realtime 동반자와 저장 요약 프롬프트 설계

